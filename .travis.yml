language: node_js
node_js:
  - "15"
jobs:
  include:
    - stage: test
      script:
        - yarn lint
    - stage: release
      if: branch = master
      script:
        - npx semantic-release@15
